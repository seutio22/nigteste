import PlacementFilaDetailPage from './Detail'

/** Etapa atual do processo em tela cheia (todas as fases do workflow). */
export default function PlacementEtapaDetailPage() {
  return <PlacementFilaDetailPage fullscreen />
}
