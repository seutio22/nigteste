# Deploy manual do frontend (demandas-web). O deploy em push na branch main está em `deploy.yml`
# para evitar dois workflows a publicar no Vercel no mesmo push.
name: 🚀 Deploy Vercel (Frontend)

on:
  workflow_dispatch:

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    name: 🌐 Deploy Frontend (Vercel)
    
    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4
      
    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: "20"
        cache: 'npm'
        cache-dependency-path: demandas-web/package-lock.json
        
    - name: 📦 Instalar dependências
      working-directory: ./demandas-web
      run: npm ci
      
    - name: 🏗️ Build do projeto
      working-directory: ./demandas-web
      run: npm run build
      
    - name: 🚀 Deploy no Vercel
      uses: amondnet/vercel-action@v25
      with:
        vercel-token: ${{ secrets.VERCEL_TOKEN }}
        vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
        vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
        working-directory: ./demandas-web
        vercel-args: '--prod'
        
    - name: 📋 URL do Deploy
      run: |
        echo "🎉 DEPLOY CONCLUÍDO!"
        echo "🌐 Frontend online no Vercel!"
        echo "📱 Acesse o dashboard do Vercel para ver a URL"
