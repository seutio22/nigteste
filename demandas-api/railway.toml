[build]
builder = "nixpacks"
buildCommand = "npm run build && echo 'BUILD COMPLETED - FORCING RECOGNITION'"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[env]
NODE_ENV = "production"

