# Em desenvolvimento o Vite usa proxy: chamadas para /api → http://localhost:4001
# OBRIGATÓRIO em produção (Vercel → Environment Variables): sem isso o login chama /api neste site e dá 404.
# URL pública da API (Railway amusing-flexibility), sem /api no final:
# VITE_API_URL=https://portal-colaborador-api-production.up.railway.app
